import { Component } from 'react';
import { BackgroundSelectorItem } from '../models';
interface BackgroundToolProps {
    api: {
        on(event: string, callback: (data: any) => void): void;
        off(event: string, callback: (data: any) => void): void;
        getCurrentStoryData(): any;
    };
}
interface BackgroundToolState {
    items: BackgroundSelectorItem[];
    selected: string;
    expanded: boolean;
}
export declare class BackgroundSelector extends Component<BackgroundToolProps, BackgroundToolState> {
    private listener;
    constructor(props: BackgroundToolProps);
    componentDidMount(): void;
    componentWillUnmount(): void;
    change: (args: {
        selected: string;
        expanded: boolean;
    }) => void;
    render(): JSX.Element;
}
export {};
